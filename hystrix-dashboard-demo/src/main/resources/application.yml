server:
  port: 8844

spring:
  application:
    name: hystrix-dashboard

eureka:
  instance:
    metadata-map:
      management.port: ${management.port}
turbine:
  aggregator:
    clusterConfig: default
  appConfig: module-consumer
  clusterNameExpression: "'default'"
